const D_PATH ='/admin';
export enum RoutesEnum {
    DASHBOARD=D_PATH,
    PRODUCT=`${D_PATH}/product`,
    PRODUCT_ADD=`${D_PATH}/product/add`,
    USER=`${D_PATH}/user`,
    SUBSCRIPTION=`${D_PATH}/subscription`,
}